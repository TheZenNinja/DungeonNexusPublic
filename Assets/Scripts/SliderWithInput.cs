using System.Collections;
using UnityEngine;
using UnityEngine.UI;

namespace Zen.UI
{
    public class SliderWithInput : MonoBehaviour
    {
        public Slider slider;
        public Text textInput;

        private void Awake()
        {
            //set up callbacks to sync values
        }
    }
}